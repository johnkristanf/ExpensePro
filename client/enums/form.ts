export enum InputType {
    INPUT = 'input',
    SELECT = 'select',
    TEXTAREA = 'textarea',
}

export enum FieldInputType {
    TEXT = 'text',
    NUMBER = 'number',
    DATE = 'date',
}
